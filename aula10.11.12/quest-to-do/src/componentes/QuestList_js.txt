import './QuestList.css';

export default function QuestList(props) {
    return (
        <div className="questlist-container">
            {props.quests.map((quest) => {
                return <p key={quest.id} className="quest-item">{quest.title}</p>
            })}
        </div>
    )
}

